# Supabase — public values, safe to expose to the browser.
# From the Supabase dashboard: Project Settings -> API.
NEXT_PUBLIC_SUPABASE_URL=https://nlhpxtrpddjnnqgxwqle.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# No service-role key here: candidate-data access goes through RLS with the
# user's session (constitution principle 6). A later phase that needs the
# service-role key keeps it server-only and out of this template.
